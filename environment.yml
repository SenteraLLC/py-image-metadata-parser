name: image-parsing
channels:
  - conda-forge
dependencies:
  - python=3.8
  # dev dependencies
  - sphinx
  - sphinx_rtd_theme
  - pytest
  - pre_commit
  # main dependencies
  - exifread
  - click
  - decorator
  - timezonefinder
  - xmltodict
  - requests
