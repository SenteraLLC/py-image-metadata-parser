[tool.poetry]
name = "imgparse"
version = "1.12.4"
description = "Python image-metadata-parser utilities"
authors = []

[tool.poetry.dependencies]
python = "^3.7"
exifread = "*"
click = "*"
decorator = "*"
timezonefinder = "*"
xmltodict = "*"
requests = "*"

pandas = {version = "*", optional = true}

[tool.poetry.dev-dependencies]
pytest = "*"
pytest-coverage= "*"
sphinx_rtd_theme = "*"
sphinx = "*"
pre_commit = "*"
requests-mock = "*"

[tool.poetry.scripts]
imgparse = "imgparse.cli:cli"

[tool.poetry.extras]
metadata_csv = ["pandas"]

[tool.isort]
line_length = 88
multi_line_output = 3
include_trailing_comma = true
known_third_party = ["click", "decorator", "exifread", "pandas", "pytest", "pytz", "requests", "timezonefinder", "xmltodict"]

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
